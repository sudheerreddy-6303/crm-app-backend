# Copy this file to .env and fill in your values

# MySQL (Railway gives you these in the MySQL service > Variables tab)
DB_HOST=tramway.proxy.rlwy.net
DB_PORT=53767
DB_USER=root
DB_PASSWORD=OIYzyyIBmGfNaQvwujrqcErsPKGmBpKG
DB_NAME=telecrm
# Server
PORT=5000

# Security - generate a long random string, e.g. run:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=put-a-long-random-string-here

# First admin account (created automatically on first run)
ADMIN_EMAIL=admin@gmail.com
ADMIN_PASSWORD=deeraj@admin

# ADDED: Application URLs

# LOCAL DEVELOPMENT (default)
# Frontend URL - used for CORS (only this origin + localhost can call the API when set)
FRONTEND_URL=http://localhost:5173
# Backend URL - for reference (this server itself)
BACKEND_URL=http://localhost:5000

# PRODUCTION (Railway) - when deploying, comment the two lines above and uncomment these:
# FRONTEND_URL=https://crm-app-production-3be3.up.railway.app
# BACKEND_URL=https://crm-app-backend-production-ddaa.up.railway.app

# Older Render URLs, kept for reference:
# FRONTEND_URL=https://sales-project-xyqb.onrender.com
# BACKEND_URL=https://sales-project-backend-b5oi.onrender.com
